# Supabase Configuration (these are public/publishable keys - safe for client-side)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# Note: Never add private keys (like SUPABASE_SERVICE_ROLE_KEY) to .env
# Private keys should only be stored as Cloud secrets for edge functions
